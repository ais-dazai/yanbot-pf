namespace YanbotPFPing;

/// <summary>
/// Copy this file to Secrets.cs (which is gitignored - never commit the
/// real values) and fill in the real API URL/token before building. This
/// is what makes the plugin "zero-config" for players: the secret lives in
/// the compiled DLL, not in anything the player has to type in-game.
///
/// ApiToken must match the PARTY_FINDER_API_TOKEN environment variable set
/// on the bot's Railway service.
/// </summary>
internal static class Secrets
{
    public const string ApiUrl = "https://awake-victory-production-0511.up.railway.app/party-full";
    public const string ApiToken = "REPLACE_ME";
}
